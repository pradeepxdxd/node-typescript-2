import { Environment } from "./env";

export const DevEnvironment : Environment = {
    // this credentials has removed because of security purpose
    port: 8000,
    db_url: "",
    jwt_secret: "",
    email: "",
    password: ""
}